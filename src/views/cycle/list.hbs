<div class="container-fluid p-8">
    <div class="row">
        {{#each cycle}}
        <div class="col-md-4 mx.auto">
            <div class="card  mb-3">
                <div class="card-body">
                    <p>{{name}} </p>
                    <p><a class="btn btn-primary" href="./list/{{id}}" role="button">Solicitar Grado &raquo;</a></p>
                </div>
            </div>
        </div>
        {{/each}}
        <div class="col-md-8">
            {{!-- <div class="table-responsive">
                <table class="table table-sm ">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Nombre</th>
                            <th scope="col">Apellido</th>
                            <th scope="col">Teléfono</th>
                            <th scope="col">Mail</th>
                            <th scope="col">Número de documento</th>
                            <th scope="col">Rol</th>
                            <th scope="col">EPS</th>
                            <th scope="col" colspan="3">Operación</th>

                        </tr>
                    </thead>
                    <tbody>
                        {{#each cycle}}
                        <tr>
                            <th scope="row">{{id}}</th>
                            <td scope="row">{{first_name}} {{middle_name}}</td>
                            <td scope="row">{{last_name}} {{second_last_name}}</td>
                            <td scope="row">{{telephone}}</td>
                            <th scope="row">{{email}}</th>
                            <td scope="row">{{document_number}}</td>
                            <th scope="row">{{role_name}}</th>
                            <td>{{eps_name}}</td>
                            <td>
                                <!--
                                <a href="./edit/{{id}}" class="btn btn col-8">
                                    <i class="fas fa-shedule"></i>
                                </a>
                                -->
                            </td>
                            <td>
                                <a href="./edit/{{id}}" class="btn btn col-8">
                                    <i class="fas fa-pen"></i>
                                </a>
                            </td>
                            <td>
                                <a href="./delete/{{id}}" class="btn btn col-8">
                                    <i class="fas fa-trash"></i>
                                </a>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div> --}}
        </div>
    </div>
</div>